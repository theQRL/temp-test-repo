version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: theqrl/qrysm-builder
    steps:
      - run: echo "Hi I'm on a Container Runner!"
      - run: ip addr
workflows:
  build-workflow:
    jobs:
      - build
