version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu-2204:2023.10.1
    resource_class: theqrl/qrysm-builder
    steps:
      - run: echo "Hi I'm on a Container Runner!"
      - run: ip addr
workflows:
  build-workflow:
    jobs:
      - build
